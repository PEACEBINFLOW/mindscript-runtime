## CLI Commands

### `run`

Run a MindScript file:

```bash
python 5-cli/5.1-mindscript_cli.py run 6-examples/6.1-simple-names.ms.md --model local:stub
Planned commands:

validate — check syntax only

plan — print execution plan

dry-run — show prompts without calling model
